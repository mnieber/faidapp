import React from 'react';

import { EffectWithoutArgs } from 'src/utils/components';

type PropsT = {};

export const SelectMetricEffect: React.FC<PropsT> = (p: PropsT) => {
  return (
    <EffectWithoutArgs
      f={() => {
      }}
    />
  );
};